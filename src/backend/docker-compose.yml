version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/postgres
      - DATABASE_URL_SYNC=postgresql://postgres:postgres@db:5432/postgres
      - REDIS_URL=redis://redis:6379/0
      - ENVIRONMENT=development
      - CORS_ORIGINS=["http://localhost:3000"]
      - JWT_ALGORITHM=HS256
      - JWT_EXPIRE_MINUTES=60
      - SUPABASE_JWT_SECRET=dev-secret-change-in-production
    depends_on:
      - db
      - redis

  db:
    # pgvector image with vector extension. For PostGIS spatial queries,
    # swap to: postgis/postgis:16-3.4 (or build a custom image with both).
    # If PostGIS is unavailable, the app falls back to Haversine + Redis GEO.
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "54322:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  pgdata:
